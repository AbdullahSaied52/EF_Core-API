# EF_Core-API
A hands-on project for applying EF Core and ASP.NET Core Web API fundamentals. Includes full CRUD operations and custom querying to practice database integration—focused purely on core API logic (authentication/security excluded for now).
